This simple utility replaces the long dash "—" with a small minus sign "-," a common misuse of LLM. It also replaces the letter "ё" with the letter "е."
Just paste your text into the window, click "process" and get the finished text as an output.
